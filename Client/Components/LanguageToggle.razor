@inject LocalizationService L
@implements IDisposable

<button class="language-toggle" @onclick="Toggle">
    @(L.IsGerman ? "🇬🇧 EN" : "🇩🇪 DE")
</button>

@code {
    protected override void OnInitialized()
    {
        L.OnLanguageChanged += StateHasChanged;
    }

    private void Toggle()
    {
        L.ToggleLanguage();
    }

    public void Dispose()
    {
        L.OnLanguageChanged -= StateHasChanged;
    }
}
